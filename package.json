{
  "name": "react-native-azure-ad-auth",
  "version": "1.0.5",
  "description": "A React Native module for Azure AD authentication endpoint. This module implements Azure AD authentication using pure React-Native using typescript.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-package": "cd lib && tsc && cp package.json ../build/dist/ && cd .. && for %I in (LICENSE README.md) do cp %I ./build/dist/",
    "publish-package": "cd build/dist && npm publish"
  },
  "author": "Renz Santos",
  "license": "MIT"  
}
